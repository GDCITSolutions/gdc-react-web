import { Typography } from '@mui/material';

export default function Accessibility() {
    return (
        <Typography paragraph>
            Future accessibility page content will be displayed here.
        </Typography>
    )
}