/**
 * Sets the document.title for a page, appending '- Streetflex'.
 * @param {string} title the title of a page.
 */
function setTitle(title) {
    document.title = `${title} - Sample`;
}

export default setTitle;
